# Also configure the existing database, storage and Gemini settings.
DATABASE_URL=
# Must match chatbot-ui. Use distinct values and keys for each environment.
RAG_JWT_ISSUER=chatbot-ui-local
RAG_JWT_AUDIENCE=tree-rag-local
# Public PEM only; actual newlines or literal \n escapes are accepted.
RAG_JWT_PUBLIC_KEY=
